## 2026-02-24
[ ] 어떤 프로그램인지 설명 (최대한 자세히)
- 사용자가 원하는 장치(PC, PLC, 서버...)들의 연결정보 노드를 트리 구조로 관리할 수 있는 프로그램
- 각 노드들의 연결 정보를 통해 ping 테스트, port 오픈 여부 상태를 확인할 수 있음
- 체크 인터벌도 각 노드별로 따로 설정할 수 있으면 아름답겠음
  * 문제 발생 시 email 전송여부, 전송조건 카운트, 전송주소(복수개), 전송주기도 노드별로 설정되면 좋겠음
- 신호등으로 보이면 좋겠네 (빨강 = 뒤졌다, 노랑 = 불안, 초록 = 정상)
- 전체 연결 로그를 볼 수 있는 탭이나 컨트롤이 있으면 편하겠음
  * 선택된 노드가 없으면 전체 노드의 연결상태 로그를 보여주고
  * 선택된 노드가 있으면 해당 노드의 연결상태 로그만 보여주고
- 트리 구조, 체크 정보는 로컬에 파일로 export, import 가능 (마지막 데이터 자동으로 load 하도록)
- windows, mac 에서 동작하는 로컬 프로그램 (사실상 windows only 로 해도 될 듯?)
- 대시보드 기능 제공 : 내가 원하는 n 개의 특정 노드만 골라 전체화면에 이쁘게 연결상태 보여주는 기능
  * 기본적으로는 전체 노드를 트리 구조로 보는거고, 대시보드는 이쁘장하게 현재 연결상태만 보여주는 보여주기식 기능
- 뭐가 있으면 더 좋을까? 진짜 유용하고 딱 필요한 거만 
- 프로그램 자체가 에러날 수 있으니 예외처리 잘 하고, 프로그램 에러 로그는 뒷단에서 잘 수집하도록
  * .txt 로 수집하면 좋겠고, yyyy-MM-dd.txt 형태로 저장

[ ] 어떤 언어로 개발하는게 유리할까?
- 우선 python 이 유리하다고 생각 됨

[ ] 모던 + 심플한 UI 
- 이미 이러한 프로그램은 글로벌하게 많이 개발되어 있음 so -> 사용하기 쉬워 보이게끔 만드는게 중요
  * 나는 토스 UI, UX 가 마음에 들더라, 뭐 깊게 생각 안하고 그냥 눌러보고 싶게 생김
- 이미 잘 만들어진 무료 프레임워크, 무료 라이브러리 최대한 활용


## 2026-02-25
[ ] 큰 틀 잡기
- python ui 프레임 워크 / 라이브러리 선정
- 아키텍처 구조


## 2026-02-26
[x] 큰 틀 잡기
- python ui 프레임 워크 / 라이브러리 선정 (PySide6)
- 아키텍처 구조 (계층형 MVC 및 QThread 이벤트 기반 구조)

## 2026-02-27
[x] PingForest 1차 주요 기능 구현 완료
- 코어 엔진: QThread 기반 백그라운드 모니터링 엔진 (PingService, PortService)
- 프리미엄 데스크톱 UI: 토스(Toss) 스타일 QSS 적용, 트리뷰(그룹/장치 관리), 노드 상세 설정 뷰
- 상태 알림: 동적 신호등 위젯 (정상-초록, 지연-주황, 실패-빨강, 대기-회색)
- 모던 대시보드: 전체화면 지원 Grid 타일 뷰
- 로컬 스토리지 보관: `tree_data.json` 형태 자동 저장 및 로드
- 로깅 수집: `yyyy-MM-dd.txt` 포맷의 애플리케이션 및 연결 상태 로그 수집 기능

## 2026-03-02
[ ] 2차 기능 구현
- 트리에서 우측 클릭하였을 땐 안에 노드를 제거할 수 있어야 함
  * 진짜 제거하겠냐는 확인창 필요하고
  * 안에 자식 노드들이 있으면, 자식들도 다 사라진다 말해주고
- 지금은, 그룹 - 노드 형태인데
  노드가 다시 노드의 부모가 될 수 있도록 해야 함
- 상태에는 ping 연결 상태따로, port 오픈 상태 따로 각각 보여줘야 함
- 네트워크 연결 로그에 주기마다 계속 실패했다/성공했다/ 응답속도 어떻다 이런 정보를 계속 더해줘야 함

[ ] 개선
- 화면 좌측 트리뷰에서 클릭 포커스가 유지되면 좋겠습니다.
  (지금은 클릭 후 1초뒤에 포커싱이 풀림)
- 트리뷰에서 IP/PORT 연결 상태를 각각 신호등으로 보이도록 변경.
- 화면 우측 하단, 네트워크 연결 로그쪽에 로그가 쌓여야 합니다.
  (지금은 콘솔에만 로그가 쌓이고 있음)


## 2026-03-03
- 노드가 부모의 역할도 하고 있는데, 그룹이 의미가 있을까?
  검토 ->
  필요없다면 -> 그룹 개념 제거

- IP/Host 에서 https://www.naver.com 과 같은 주소도 체크 되도록

- 트리뷰에서 공백 우클릭 시 -> 최상위 노드 만들기
- 특정 노드 우클릭 시 -> 삭제 / 하위 노드 만들기

- 하단, 하위 노드 추가 불필요해 보임. 제거
  대신, 해당 트리 정보를 .json 으로 export / import 하는 기능 추가
  
## 2026-03-05
1) 자잘한 버그 수정
  - 노드 위에 마우스를 올리고 있을 때, 노드 배경이 살짝 어두워 지는 효과가 생기는데
    체크 주기 마다 이 어두어 지는 효과가 사라지는 버그
  
  - A 노드를 좌 클릭해 포커싱을 준 뒤, 
    B 노드를 우 클릭 해 포커싱을 옮겼지만
    체크 주기가 되면 포커싱 UI 가 A 노드로 변경 됩니다.
    (우클릭해 놓은 B 로 오른쪽 메뉴는 잘 펼쳐져 있음. 단순 UI 복귀 문제로 보임)

2) 노드 상세 설정에 "대시보드로 보내기" 체크박스를 추가하고
   여기 추가된 항목들만 대시보드에서 볼 수 있도록 변경

3) 노드 상세 설정에서 "대시보드 색상", "대시보드 아이콘" 등을 설정할 수 있도록
   변경

4) 대시보드 보기 -> 로 띄워진 팝업창에서
   전체화면 전환 버튼을 눌러 전체화면이 되었으면
   버튼 이름은 "전체화면 전환" -> "창모드 전환" 으로 변경
   
5) 대시보드 타일 에서도 역시나 IP상태, PORT 상태 모두를 보여줘야 함

6) 모니터링 대시보드 글자 대신에 
   yyyy-MM-dd hh:mm:ss 시간을 노출
  

## 2026-03-07
1) 노드 상세 설정에서 대시보드 색상을 팔레트에서 고를 수 있도록 변경

2) 노드 상세 설정에서 대시보드 아이콘을 고를 수 있도록 변경
   약 30개 정도의 아이콘을 준비해두고, 그 중에서 고를 수 있도록

3) 대시보드 화면에서 각 타일의 테두리가 살짝 자글자글하게 보임
   정확하게 보이도록 변경

4) 대시보드 화면에서 상단 타이틀 옆에 있는 신호등 제거
   사유 : ping, port 상태를 각각 보여주므로 불필요

5) 네트워크 연결 로그 부분이, 노드를 클릭할 때 마다 초기화 되어 버림
   쌓인게 유지 되었으면 좋겠음
   프로그램 재시작 시 날라가는건 ok

6) 네트워크 연결 로그에서, 내가 포커싱 준 노드에 대한 정보만
   필터링 되어 보이면 좋겠음
   ex) naver 노드 선택 시 naver 관련 로그만 보여 줘야 함
   * 프로그램 시작 시 기본적으로 최상단 노드에 포커스 주는 걸로 하고
   * 포커스는 1개로만 유지 하게 하고
   * 전체 로그는 못보는 걸로 해야 하겠음

7) 네트워크 연결 로그 export 기능을 만들고 싶음
   .txt 파일로 떨어질 것이고,
   위치는 사용자가 정할 수 있도록
   기본 파일명은 이름_yyyy_MM_dd 형태로 제공


+
8) 노드 상세 설정 -> 색상 선택 팔레트는 좋은데
   내가 뭘 선택 했는지 그 색상도 함께 보이면 좋겠습니다.

9) 노드 상세 설정 -> 아이콘 선택 정상 동작 하지 않음

10) 대시보드 보기 정상 동작하지 않음

11) 대시보드 색상, 대시보드 아이콘 쪽 textBox 컨트롤 width 동일하게 맞추어
    무게감 통일
    